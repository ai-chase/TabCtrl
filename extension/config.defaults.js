// config.local.js — TabCtrl personal overrides (optional)
// This file is tracked in the repo and contains safe empty defaults.
// To override, set values in your OWN config.local.js (which is gitignored).
// When this file is absent, app.js falls back to the empty values below.

const LOCAL_LANDING_PAGE_PATTERNS = [];  // optional extra landing page hostnames
const LOCAL_PINNED_TABS = [];            // optional always-visible tabs
